name: mem0

services:
  mem0:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8000"
    labels:
      - traefik.enable=true
      - traefik.http.routers.mem0-api-http.rule=Host(`mem0-api.petrenko.cv`)
      - traefik.http.routers.mem0-api-http.entrypoints=http
      - traefik.http.routers.mem0-api-http.service=mem0-api
      - traefik.http.routers.mem0-api-https.rule=Host(`mem0-api.petrenko.cv`)
      - traefik.http.routers.mem0-api-https.entrypoints=https
      - traefik.http.routers.mem0-api-https.tls=true
      - traefik.http.routers.mem0-api-https.service=mem0-api
      - traefik.http.services.mem0-api.loadbalancer.server.port=8000
      - traefik.docker.network=coolify
    networks:
      - mem0_network
      - coolify
    depends_on:
      postgres:
        condition: service_started
    command: ["sh", "/app/start.sh"]
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=
      - OPENAI_API_KEY=${OPENAI_API_KEY:?}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-}
      - POSTGRES_HOST=mem0-postgres
      - POSTGRES_PORT=5432
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_COLLECTION_NAME=${POSTGRES_COLLECTION_NAME:-memories}
      - APP_DB_NAME=mem0_app
      - JWT_SECRET=${JWT_SECRET:?}
      - ADMIN_API_KEY=${ADMIN_API_KEY:-}
      - AUTH_DISABLED=false
      - DASHBOARD_URL=${DASHBOARD_URL:?}
      - MEM0_DEFAULT_LLM_MODEL=${MEM0_DEFAULT_LLM_MODEL:-gpt-4.1-nano-2025-04-14}
      - MEM0_DEFAULT_EMBEDDER_MODEL=${MEM0_DEFAULT_EMBEDDER_MODEL:-text-embedding-3-small}
      - MEM0_TELEMETRY=${MEM0_TELEMETRY:-false}
      - REQUEST_LOG_RETENTION_DAYS=${REQUEST_LOG_RETENTION_DAYS:-30}
  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "3000"
    labels:
      - traefik.enable=true
      - traefik.http.routers.mem0-ui-http.rule=Host(`mem0.petrenko.cv`)
      - traefik.http.routers.mem0-ui-http.entrypoints=http
      - traefik.http.routers.mem0-ui-http.service=mem0-ui
      - traefik.http.routers.mem0-ui-https.rule=Host(`mem0.petrenko.cv`)
      - traefik.http.routers.mem0-ui-https.entrypoints=https
      - traefik.http.routers.mem0-ui-https.tls=true
      - traefik.http.routers.mem0-ui-https.service=mem0-ui
      - traefik.http.services.mem0-ui.loadbalancer.server.port=3000
      - traefik.docker.network=coolify
      - caddy_0=https://mem0.petrenko.cv
      - caddy_0.encode=zstd gzip
      - caddy_0.handle_path=/*
      - caddy_0.handle_path.0_reverse_proxy={{upstreams 3000}}
      - caddy_0.header=-Server
      - caddy_0.try_files={path} /index.html /index.php
      - caddy_ingress_network=yydnbjtfkeb4rxm5jewd28qf
    networks:
      - mem0_network
      - coolify
    environment:
      - NEXT_PUBLIC_API_URL=${API_URL:?}
      - API_INTERNAL_URL=http://mem0:8000
      - NEXT_PUBLIC_INSTANCE_NAME=${NEXT_PUBLIC_INSTANCE_NAME:-Mem0}
    depends_on:
      mem0:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    shm_size: "256mb"
    networks:
      mem0_network:
        aliases:
          - mem0-postgres
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_db_pg16_v2:/var/lib/postgresql/data
      - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro

volumes:
  postgres_db_pg16_v2:

networks:
  coolify:
    external: true
  mem0_network:
    driver: bridge
